// it should be possible to just write the file you search for to search in the current directory
// also you should be able to search for paths like     C-Projects/search      or         C-Projects/search/

1. suche nach Applications
 -> resutl = /home/marc/Applications
2. suche in result nach packettracer
3. Wenn packettracer in der ERSTEN ITERRATION nicht gefunden wird, dann gebe ("%s nicht gefunden", packettracer) aus 

RC_COPY SACHVERHALT LÖSEN 
STATIC INT RESETTEN???
-> maybe instead add a int pointer?? to the parameters

  if (argc != 2) {
    printf("Usage: search <file name>");
    return 1; 
  }
   argv[1] ==> the directory to search in. argv[2] the directory/ file to search for 
  searchFile(argv[1], argv[2]);
